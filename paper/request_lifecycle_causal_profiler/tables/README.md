# Tables

Keep hand-written or generated table snippets here.

The template's generic paper-asset export path writes generated LaTeX snippets
under `tables/generated/`. The manuscript can `\input{}` those files without
depending directly on ad hoc result paths.

Derived repositories can add curated tables beside the generated ones when they
need tighter narrative control.