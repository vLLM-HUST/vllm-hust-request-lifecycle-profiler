# Manual Tables

Keep curated hand-maintained table snippets here.

This directory is reserved for tables that should remain stable under human
editing and must not be overwritten by `make paper-assets`. Generated snippets
belong under `tables/generated/` instead.