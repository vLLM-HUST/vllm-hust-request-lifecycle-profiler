# Example Live Results

Keep sample live-serving or online results here when a derived repository wants
to distinguish them from offline analysis outputs under the main `results/`
directory.

The template's generic `paper-assets` path scans this directory separately and
records its files under the `live_results` source bucket in the generated
manifest and summaries.