# Claim Ledger

| Claim | Current status | Required evidence | Forbidden wording |
| --- | --- | --- | --- |
| The fixed Issue #19 worker-exit-last/pending-transfer specialty scenario does not justify implementing lifecycle reconciliation. | Supported as an evidence-valid `real-online` mechanism `NO_GO`: all 10 matched fault/control pairs are evidence-valid, resource pathology appears in 1/10, and matched latency pathology in 0/10, below the preregistered 8/10 gate. Reconciliation remains disabled. | Sanitized per-arm analyses and summary under `.benchmarks/results/m0_issue19_public/20260822-sanitized-corrected-paired-10/`, verified by `make issue19-public-evidence-verify`; the unchanged raw source remains in controlled local custody. | Do not generalize this result to all lifecycle failures, all reconciliation designs, disconnect, timeout, receipt loss, multi-Worker, multi-rank, or the Request Lifecycle topic. Do not retune the workload, capacity, fault order, or gate and call the result a retry of Issue #19. |
| Issue #19 demonstrates a lifecycle decision gate that can stop an unsupported optimization. | Supported narrowly: complete request/resource evidence distinguishes expected fault noncompletion, invalid evidence, and one observed state-conservation failure; the fixed reproduction gate rejects treatment despite that single event. | The Issue #19 evidence above plus the preregistered gate and evaluator output. This is decision-impact evidence, not treatment-effect evidence. | Do not claim that complete tracing or the longest stage proves a causal root cause, that the profiler improved performance, or that the single failure establishes a repeatable optimization opportunity. |
| The upstream profiler has an executable offline KV-recovery phase model and decomposition. | Supported as `offline-unit` readiness on `feature/kv-recovery-integration`, which composes merged PR #4 (`15717eae2630e80c11b113ccaeb3422871b35b40`) with frozen parent checkpoint `9f1464b8d017ef48e66b8b4c9bd4a5a37fdc563d`. The combined gate passed 67 focused and 94 full tests plus targeted Ruff check/format; frozen P0 and four GO-bound files remain unchanged. | Integration source/tests, recorded affected hashes, and rerun CPU gates establish local offline availability. A reviewed optional profile, runtime call sites, and a controlled runtime trace are still required for capture claims. | Do not call the offline model runtime-integrated, NPU-tested, an online result, or a performance improvement. |
| The PR #4 NPU6 preflight was BLOCKED and launched no service. | Supported as `readiness-only`: the recorded blockers were a failed models endpoint, missing trace-export path, and no NPU6 process. This says only that the environment was not admitted. It is a different snapshot from the older checked-in READY proxy run. | `run_metadata.json` plus exactly one `BLOCKED.txt` marker and `result_valid_for_paper_claims=false`; a later version-aware READY preflight is required before any controlled service run. | Do not infer latency, throughput, tiering behavior, profiler overhead, negative performance, or current READY state from this blocked preflight. |
| Lifecycle events can be converted into deterministic stage spans. | Supported by unit tests and synthetic schema coverage. | `PYTHONPATH=src pytest -q`; `.benchmarks/results/synthetic_fault_injection_env/matrix.json` with 0.0 missing-event rate for each scenario. | Do not call this causal proof by itself. |
| The profiler distinguishes tokenizer, queueing, prefill, decode, KV pressure, streaming backpressure, and cleanup in deterministic synthetic traces. | Supported as `simulation/model` evidence only. Latest valid result: `.benchmarks/results/synthetic_fault_injection_env/` (`accuracy=1.0`, `false_positive_count=0`, `false_negative_count=0`, `scenario_count=7`). | NPU6 controlled fault-injection matrix with known ground truth before claiming live diagnosis accuracy. | Do not claim general diagnosis accuracy from synthetic traces only. |
| The repository has a concrete NPU6 existing-server trace-probe contract. | Supported for client-observed proxy traces. Latest stable run: `.benchmarks/results/npu6_existing_server_trace_probe_repeated_smoke/` used 1 warmup request plus 12 measured streaming requests on NPU6; all measured requests succeeded, emitted 117 lifecycle events, and measured TTFT p50/p95/p99 at 83.43/85.76/86.65 ms. `.benchmarks/results/npu6_trace_probe_preflight/` contains `READY.txt` with trace schema validation passing. | `docs/npu6_trace_probe_runbook.md`; `.benchmarks/preflight_npu6_trace_probe.py`; `.benchmarks/run_existing_server_trace_probe.py`; `tests/test_npu6_preflight.py`; `tests/test_existing_server_trace_probe.py`; `READY.txt`, `run_metadata.json`, and `summary.json`. | Do not claim this is an internal vLLM scheduler/KV trace; it is a client-observed lifecycle proxy until runtime hooks are integrated. |
| Client-observed NPU6 traces can be converted into per-request stage diagnosis. | Supported as `derived-artifact` by `.benchmarks/results/npu6_trace_diagnosis/`, derived from `.benchmarks/results/npu6_existing_server_trace_probe_repeated_smoke/probe_results.json`: 13 requests and 117 events have 0.0 missing-event rate; among 12 measured requests, the dominant client-visible span is `decode` for 12/12, with decode-proxy p95 98.75 ms and prefill-proxy p95 85.50 ms. | `diagnosis.json`, `summary.json`, `request_diagnosis.csv`, and `run_metadata.json`; parent commit `6fad382`; evidence label `derived-artifact`; `dirty_excluding_output_dir=false`. | Do not call this internal decode root cause, scheduler attribution, or live fault diagnosis; it is a client-visible stage diagnosis derived from proxy events. |
| Client-probe lifecycle event construction has low visible overhead on the matched NPU6 smoke workload. | Supported as `existing-server-probe` only by `.benchmarks/results/npu6_existing_server_trace_overhead_smoke/`: matched no-trace and trace modes each sent 12 measured streaming requests with 12/12 success. Trace mode emitted 117 client-observed lifecycle events. Relative to no-trace, trace mode changed TTFT p95 by +4.07 ms and latency p95 by +4.03 ms. | `overhead_summary.json`, per-mode `summary.json`, and per-mode `run_metadata.json`; parent commit `801cde8`; `dirty_excluding_output_dir=false`; modes `no-trace` and `trace`. | Do not call this internal runtime hook overhead, memory overhead, or end-to-end profiler overhead inside vLLM-HUST. It only measures client-probe event construction around an existing managed server. |
| Client-visible diagnosis changes under controlled slow-stream backpressure. | Supported as `existing-server-probe` plus `derived-artifact` by `.benchmarks/results/npu6_existing_server_slow_stream_trace_smoke/` and `.benchmarks/results/npu6_slow_stream_trace_diagnosis/`: 1 warmup plus 4 measured requests used `proxy_stage_mode=streaming-proxy`, `per_chunk_read_delay_ms=80`, and 64 output tokens. All measured requests succeeded; TTFT p95 was 81.14 ms while latency p95 was 5208.20 ms. Derived diagnosis attributed 4/4 measured requests to `streaming`, with streaming span p95 5127.09 ms, prefill p95 80.86 ms, and missing-event-rate p95 0.0. | `probe_results.json`, `summary.json`, `run_metadata.json`, `diagnosis.json`, and `request_diagnosis.csv`; probe parent commit `6cdbdc9`; diagnosis parent commit `1a0f814`; evidence labels `existing-server-probe` and `derived-artifact`; generated table/CSV from `make paper-assets`. | Do not call this internal streaming backpressure, scheduler diagnosis accuracy, or runtime hook evidence; the delay is intentionally introduced in the client probe and proves only client-visible stage discrimination. |
| Lifecycle diagnosis changes the next optimization action compared with timer-only symptoms. | Supported as a `derived-artifact` by `paper/request_lifecycle_causal_profiler/results/paper_assets_latest/npu6_diagnosis_decision_impact.csv`: normal proxy traces point to internal decode/prefill validation, while slow-stream proxy traces keep TTFT low but move latency p95 to 5208.20 ms and point to streaming backpressure isolation before compute-stage tuning. | `npu6_diagnosis_decision_impact.csv` and `tables/generated/npu6_diagnosis_decision_impact.tex` from `make paper-assets`; source evidence remains `.benchmarks/results/npu6_trace_diagnosis/` and `.benchmarks/results/npu6_slow_stream_trace_diagnosis/`. | Do not call this a new online experiment, internal root-cause proof, or measured time-to-root-cause improvement; it is a derived decision queue over client-visible proxy traces. |
| Internal runtime hook sites can write the full lifecycle schema. | Supported as `existing-server-probe` runtime evidence by `.benchmarks/results/npu6_runtime_hooks_low_overhead_enabled_smoke/`: the pinned `third_party/vllm-hust` submodule runs branch `feature/request-lifecycle-profiler-runtime-hooks-faculty`, commit `0daab7a30`, and writes 117 internal JSONL events for 13 external request chains. All chains include `received`, `tokenized`, `queued`, `scheduled`, `prefill_done`, `first_token`, `decode_done`, `stream_done`, and `cleanup_done`; `missing_stage_counts` is empty. | `runtime_trace.jsonl`, `probe_results.json`, `summary.json`, and `run_metadata.json` in `.benchmarks/results/npu6_runtime_hooks_low_overhead_enabled_smoke/`; submodule branch `feature/request-lifecycle-profiler-runtime-hooks-faculty`; parent tests and runtime-carrier `py_compile`/ruff checks. | Do not claim diagnosis accuracy, memory overhead, or TPOT yet. This proves schema-compatible full lifecycle capture on the NPU6 smoke path, not broad workload generality. |
| Hook-disabled/enabled runtime comparisons now have a reproducible run queue and chain-aware aggregator. | Supported as `derived-artifact` by `.benchmarks/results/npu6_runtime_hook_pair_plan/`: both source rows loaded with clean metadata. Hook-disabled sent 12/12 successful measured requests with TTFT p95 80.94 ms and latency p95 179.48 ms. Hook-enabled sent 12/12 successful measured requests, wrote 29,159 bytes of runtime trace, recorded 13/13 complete chains, and measured TTFT p95 78.80 ms and latency p95 180.41 ms. The audit deltas are -2.14 ms TTFT p95 and +0.93 ms latency p95. | `runtime_hook_pair_plan.json`, `summary.json`, and `run_metadata.json`; source directories `.benchmarks/results/npu6_runtime_hooks_disabled_low_overhead_baseline/` and `.benchmarks/results/npu6_runtime_hooks_low_overhead_enabled_smoke/`; unit tests for pair-plan loading and runtime trace coverage. | Do not call the derived pair-plan a standalone online run. Its deltas summarize the two source `existing-server-probe` runs and do not include memory overhead or TPOT yet. |
| Full lifecycle tracing overhead is bounded on the matched NPU6 smoke workload after the low-overhead sink change. | Supported narrowly by the paired NPU6 runtime-hook smoke: hook-enabled minus hook-disabled is -3.66 ms TTFT p50, -2.14 ms TTFT p95, -4.28 ms latency p50, and +0.93 ms latency p95, with 12/12 success in both modes. The sink no longer opens and closes the JSONL file for every event; it reuses an append fd in the serving process. | Same paired source directories and `.benchmarks/results/npu6_runtime_hook_pair_plan/summary.json`; next required evidence is memory/HBM snapshots and a larger controlled-fault matrix. | Do not claim zero overhead, memory overhead, TPOT overhead, or broad workload generality yet; this is a small warmup-controlled smoke over one shared workload shape. |
| Slow-stream controlled fault can be observed with both client proxy diagnosis and internal runtime hook coverage. | Supported by `.benchmarks/results/npu6_runtime_hooks_slow_stream_fault_smoke/` plus derived diagnosis `.benchmarks/results/npu6_runtime_hooks_slow_stream_fault_diagnosis/`: the hook-enabled NPU6 service ran the slow-stream shape with 1 warmup and 4 measured requests. All measured requests succeeded; client TTFT p95 was 207.75 ms and latency p95 was 5334.74 ms under the 80 ms per-chunk read delay. Client-visible diagnosis attributes 4/4 measured requests to `streaming` with streaming-span p95 5126.99 ms. The internal runtime hook wrote 45 events across 5/5 complete chains with all nine stages present. | `probe_results.json`, `summary.json`, `run_metadata.json`, `client_proxy_trace.jsonl`, `runtime_trace.jsonl`, `runtime_trace_summary.json`, and derived diagnosis files; parent commit `133042e`; workload commit `76e24c8`; evidence labels `existing-server-probe` and `derived-artifact`. | Do not claim internal streaming root-cause accuracy yet. The client-side injected slow read is the known fault, and the current runtime summary proves hook coverage during that fault, not that internal spans alone identify the fault without the client anchor. |
| Decode-heavy controlled fault can be observed with both client proxy diagnosis and internal runtime hook coverage. | Supported by `.benchmarks/results/npu6_runtime_hooks_structured_decode_fault_smoke/` plus derived diagnosis `.benchmarks/results/npu6_runtime_hooks_structured_decode_fault_diagnosis/`: the hook-enabled NPU6 service ran `shared_scenario_structured_agent_decode` with 1 warmup, 4 measured requests, and 128 requested output tokens. All measured requests succeeded; client TTFT p95 was 150.96 ms and latency p95 was 2135.82 ms. Client-visible diagnosis attributes 4/4 measured requests to `decode` with decode-span p95 1986.54 ms and streaming p95 0.0 ms. The internal runtime hook wrote 45 events across 5/5 complete chains with all nine stages present. | `probe_results.json`, `summary.json`, `run_metadata.json`, `client_proxy_trace.jsonl`, `runtime_trace.jsonl`, `runtime_trace_summary.json`, and derived diagnosis files; parent commit `5646a5f`; workload commit `76e24c8`; evidence labels `existing-server-probe` and `derived-artifact`. | Do not claim internal decode root-cause accuracy yet. This is a known decode-heavy workload plus client-visible diagnosis and runtime-hook coverage; stronger claims need internal span analysis or causal-rule comparison against raw timers. |
| Prompt-heavy low-output controlled fault can be observed with both client proxy diagnosis and internal runtime hook coverage. | Supported by `.benchmarks/results/npu6_runtime_hooks_prompt_heavy_low_output_smoke/` plus derived diagnosis `.benchmarks/results/npu6_runtime_hooks_prompt_heavy_low_output_diagnosis/`: the hook-enabled NPU6 service ran `shared_scenario_structured_agent_decode` with 1 warmup, 8 measured requests, and 4 requested output tokens. All measured requests succeeded; client TTFT p95 was 133.89 ms and latency p95 was 170.13 ms. Client-visible diagnosis attributes 8/8 measured requests to `prefill`, with prefill-span p95 133.66 ms, decode-span p95 36.70 ms, and streaming p95 0.0 ms. The internal runtime hook wrote 81 events across 9/9 complete chains with all nine stages present. | `probe_results.json`, `summary.json`, `run_metadata.json`, `client_proxy_trace.jsonl`, `runtime_trace.jsonl`, `runtime_trace_summary.json`, and derived diagnosis files; parent commit `24ec4cf`; workload commit `76e24c8`; evidence labels `existing-server-probe` and `derived-artifact`. | Do not claim internal prefill root-cause accuracy yet. This closes the valid prefill-dominant live-fault coverage gap, but it still needs raw-timer comparison, internal span analysis, and a KV-pressure case before broad causal diagnosis claims. |
| Concurrency-sensitive structured-agent workload exposes a tail localized to the coarse runtime prefill span. | Supported by `.benchmarks/results/npu6_runtime_hooks_concurrency_sweep/`: the hook-enabled NPU6 service ran `shared_scenario_structured_agent_decode` with 1 warmup, 3 repeats x 3 measured requests, 8 requested output tokens, and measured concurrency 1/2/3. All 27 measured requests succeeded. Concurrency 1 is mostly `decode` dominant (7 decode, 2 prefill) with TTFT p95 163.30 ms. Concurrency 2 exposes a severe prefill/TTFT tail: 8/9 measured requests are `prefill` dominant, TTFT p95 is 12299.08 ms, latency p95 is 12404.06 ms, and client-visible prefill-span p95 is 12298.88 ms. Concurrency 3 remains `prefill` dominant for 9/9 measured requests but with TTFT p95 131.61 ms. The combined runtime hook trace records 270 events across 30/30 complete chains with all nine stages present and no missing-stage counts. Post-hoc internal span analysis localizes the concurrency-2 tail to the runtime prefill span: runtime prefill p95 is 12261.98 ms at concurrency 2, compared with 150.31 ms at concurrency 1 and 106.94 ms at concurrency 3; concurrency 3 instead shows higher queueing p95 at 49.71 ms. The chain-level anomaly artifact `.benchmarks/results/npu6_runtime_hooks_concurrency_sweep/concurrency_anomaly_analysis/` is `derived-artifact` evidence at parent commit `6fee671` with `dirty_excluding_output_dir=false`; it holds prompt tokens (2335), generation tokens (8), and cached-token ratio p50 (0.987) constant, and finds the only two >1 s runtime-prefill outliers in concurrency 2. | Per-concurrency `probe_results.json`, `summary.json`, `client_proxy_trace.jsonl`, derived diagnosis directories, `runtime_trace.jsonl`, `runtime_trace_summary.json`, `concurrency_sweep_summary.json`, `concurrency_sweep_summary.md`, `runtime_internal_span_analysis.json`, `runtime_internal_span_analysis.md`, `runtime_internal_spans.csv`, plus `concurrency_anomaly_analysis/{summary.json,request_rows.json,anomaly_analysis.md,run_metadata.json}`; source parent commit `89c1a4a`; anomaly parent commit `6fee671`; workload commit in per-run metadata; evidence labels `existing-server-probe` and `derived-artifact`. | Do not call this KV allocator proof yet. The current evidence localizes the tail to the coarse runtime prefill span under a fixed prompt/output/cache shape, but it does not distinguish KV allocation, graph capture, batch transition, or prefill kernel execution without finer internal fields or counters. |
| Checked-in NPU6 controlled-fault artifacts can be audited as a benchmark-owned matrix. | Supported as `derived-artifact` only by `.benchmarks/results/npu6_controlled_fault_matrix/`: the aggregator records 4 available checked-in rows (slow streaming backpressure, decode-heavy, prompt-heavy prefill, and the concurrency-2 coarse-prefill observation) with attribution agreement 4/4 over available rows and missing-event-rate p95 max 0.0. The concurrency row is an observation, not controlled-fault ground truth. The audit also records that queue pressure, KV pressure, and cleanup do not yet have benchmark-owned repo-launched NPU6 ground truth. Raw-log and manual time-to-root-cause baselines are explicitly missing/not measured. | `.benchmarks/analyze_controlled_fault_matrix.py`; `.benchmarks/results/npu6_controlled_fault_matrix/{run_metadata.json,ground_truth.json,matrix.json,summary.json,fault_matrix.csv,matrix.md}`; `tests/test_controlled_fault_matrix.py`; generated paper CSV/table from `make paper-assets`. | Do not claim live internal diagnosis accuracy, time-to-root-cause improvement, or full controlled-fault coverage from this matrix. It is an audit/readiness artifact over existing-server probes and derived diagnoses. |
| Some long-context candidate workloads exceed the current NPU6 service boundary before runtime hook attribution can run. | Supported as invalid/boundary evidence by `.benchmarks/results/npu6_runtime_hooks_long_prefill_fault_smoke/` and `.benchmarks/results/npu6_runtime_hooks_decode_heavy_fault_smoke/`: `shared_scenario_rag_followup_long_context` and `shared_preemption_resume_long_decode` each produced 4/4 failed measured requests and no positive latency samples under the current 4096-token NPU6 service profile. Both directories contain `FAILED.txt`. | `FAILED.txt`, `run_metadata.json`, `summary.json`, and `probe_results.json` in both directories. | Do not use these directories as controlled-fault attribution evidence or as proof the profiler fails; they are workload/service-boundary findings that should guide the next prefill-pressure workload design. |
| The profiler reduces time-to-root-cause compared with raw logs. | Not yet measured. | Human or scripted diagnosis comparison on real traces. | Do not claim performance improvement from profiler-only runs. |
