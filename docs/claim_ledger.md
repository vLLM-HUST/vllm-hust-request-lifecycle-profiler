# Claim Ledger

| Claim | Current status | Required evidence | Forbidden wording |
| --- | --- | --- | --- |
| Lifecycle events can be converted into deterministic stage spans. | Supported by unit tests and synthetic schema coverage. | `PYTHONPATH=src pytest -q`; `.benchmarks/results/synthetic_fault_injection_env/matrix.json` with 0.0 missing-event rate for each scenario. | Do not call this causal proof by itself. |
| The profiler distinguishes tokenizer, queueing, prefill, decode, KV pressure, streaming backpressure, and cleanup in deterministic synthetic traces. | Supported as `simulation/model` evidence only. Latest valid result: `.benchmarks/results/synthetic_fault_injection_env/` (`accuracy=1.0`, `false_positive_count=0`, `false_negative_count=0`, `scenario_count=7`). | NPU6 controlled fault-injection matrix with known ground truth before claiming live diagnosis accuracy. | Do not claim general diagnosis accuracy from synthetic traces only. |
| The repository has a concrete NPU6 existing-server trace-probe contract. | Supported for client-observed proxy traces. Latest stable run: `.benchmarks/results/npu6_existing_server_trace_probe_repeated_smoke/` used 1 warmup request plus 12 measured streaming requests on NPU6; all measured requests succeeded, emitted 117 lifecycle events, and measured TTFT p50/p95/p99 at 83.43/85.76/86.65 ms. `.benchmarks/results/npu6_trace_probe_preflight/` contains `READY.txt` with trace schema validation passing. | `docs/npu6_trace_probe_runbook.md`; `.benchmarks/preflight_npu6_trace_probe.py`; `.benchmarks/run_existing_server_trace_probe.py`; `tests/test_npu6_preflight.py`; `tests/test_existing_server_trace_probe.py`; `READY.txt`, `run_metadata.json`, and `summary.json`. | Do not claim this is an internal vLLM scheduler/KV trace; it is a client-observed lifecycle proxy until runtime hooks are integrated. |
| Client-observed NPU6 traces can be converted into per-request stage diagnosis. | Supported as `derived-artifact` by `.benchmarks/results/npu6_trace_diagnosis/`, derived from `.benchmarks/results/npu6_existing_server_trace_probe_repeated_smoke/probe_results.json`: 13 requests and 117 events have 0.0 missing-event rate; among 12 measured requests, the dominant client-visible span is `decode` for 12/12, with decode-proxy p95 98.75 ms and prefill-proxy p95 85.50 ms. | `diagnosis.json`, `summary.json`, `request_diagnosis.csv`, and `run_metadata.json`; parent commit `6fad382`; evidence label `derived-artifact`; `dirty_excluding_output_dir=false`. | Do not call this internal decode root cause, scheduler attribution, or live fault diagnosis; it is a client-visible stage diagnosis derived from proxy events. |
| Client-probe lifecycle event construction has low visible overhead on the matched NPU6 smoke workload. | Supported as `existing-server-probe` only by `.benchmarks/results/npu6_existing_server_trace_overhead_smoke/`: matched no-trace and trace modes each sent 12 measured streaming requests with 12/12 success. Trace mode emitted 117 client-observed lifecycle events. Relative to no-trace, trace mode changed TTFT p95 by +4.07 ms and latency p95 by +4.03 ms. | `overhead_summary.json`, per-mode `summary.json`, and per-mode `run_metadata.json`; parent commit `801cde8`; `dirty_excluding_output_dir=false`; modes `no-trace` and `trace`. | Do not call this internal runtime hook overhead, memory overhead, or end-to-end profiler overhead inside vLLM-HUST. It only measures client-probe event construction around an existing managed server. |
| Client-visible diagnosis changes under controlled slow-stream backpressure. | Supported as `existing-server-probe` plus `derived-artifact` by `.benchmarks/results/npu6_existing_server_slow_stream_trace_smoke/` and `.benchmarks/results/npu6_slow_stream_trace_diagnosis/`: 1 warmup plus 4 measured requests used `proxy_stage_mode=streaming-proxy`, `per_chunk_read_delay_ms=80`, and 64 output tokens. All measured requests succeeded; TTFT p95 was 81.14 ms while latency p95 was 5208.20 ms. Derived diagnosis attributed 4/4 measured requests to `streaming`, with streaming span p95 5127.09 ms, prefill p95 80.86 ms, and missing-event-rate p95 0.0. | `probe_results.json`, `summary.json`, `run_metadata.json`, `diagnosis.json`, and `request_diagnosis.csv`; probe parent commit `6cdbdc9`; diagnosis parent commit `1a0f814`; evidence labels `existing-server-probe` and `derived-artifact`; generated table/CSV from `make paper-assets`. | Do not call this internal streaming backpressure, scheduler diagnosis accuracy, or runtime hook evidence; the delay is intentionally introduced in the client probe and proves only client-visible stage discrimination. |
| Lifecycle diagnosis changes the next optimization action compared with timer-only symptoms. | Supported as a `derived-artifact` by `paper/request_lifecycle_causal_profiler/results/paper_assets_latest/npu6_diagnosis_decision_impact.csv`: normal proxy traces point to internal decode/prefill validation, while slow-stream proxy traces keep TTFT low but move latency p95 to 5208.20 ms and point to streaming backpressure isolation before compute-stage tuning. | `npu6_diagnosis_decision_impact.csv` and `tables/generated/npu6_diagnosis_decision_impact.tex` from `make paper-assets`; source evidence remains `.benchmarks/results/npu6_trace_diagnosis/` and `.benchmarks/results/npu6_slow_stream_trace_diagnosis/`. | Do not call this a new online experiment, internal root-cause proof, or measured time-to-root-cause improvement; it is a derived decision queue over client-visible proxy traces. |
| Internal runtime hook sites can write the same lifecycle schema. | Supported as first `existing-server-probe` runtime evidence by `.benchmarks/results/npu6_runtime_hooks_enabled_smoke/`: the pinned `third_party/vllm-hust` submodule runs branch `feature/request-lifecycle-profiler-runtime-hooks-faculty`, commit `7d5406c5a`, and writes 78 internal JSONL events for 13 requests. Covered runtime stages are `scheduled`, `prefill_done`, `first_token`, `decode_done`, `stream_done`, and `cleanup_done`, each observed 13 times. Coverage gap: `received`, `tokenized`, and `queued` did not appear on this OpenAI serving path and need an entry-path hook fix before claiming full lifecycle coverage. | `runtime_trace.jsonl`, `probe_results.json`, `summary.json`, and `run_metadata.json` in `.benchmarks/results/npu6_runtime_hooks_enabled_smoke/`; submodule branch `feature/request-lifecycle-profiler-runtime-hooks-faculty`; parent tests `make test`; runtime-carrier `py_compile` and ruff checks. | Do not claim full lifecycle coverage, diagnosis accuracy, or memory overhead yet. Internal evidence currently starts at scheduler visibility and must be paired with client-observed anchors. |
| Hook-disabled/enabled runtime comparisons now have a reproducible run queue and aggregator. | Supported as `derived-artifact` by `.benchmarks/results/npu6_runtime_hook_pair_plan/`: both source rows loaded with clean metadata. Hook-disabled sent 12/12 successful measured requests with TTFT p95 78.63 ms and latency p95 177.63 ms. Hook-enabled sent 12/12 successful measured requests, wrote 18,643 bytes of runtime trace, and recorded TTFT p95 88.65 ms and latency p95 193.71 ms. The audit deltas are +10.03 ms TTFT p95 and +16.08 ms latency p95. | `runtime_hook_pair_plan.json`, `summary.json`, and `run_metadata.json`; source directories `.benchmarks/results/npu6_runtime_hooks_disabled_smoke/` and `.benchmarks/results/npu6_runtime_hooks_enabled_smoke/`; unit tests for pair-plan loading and runtime trace coverage. | Do not call the derived pair-plan a standalone online run. Its deltas summarize the two source `existing-server-probe` runs and do not include memory overhead or TPOT yet. |
| Internal trace overhead on TTFT/latency is bounded on the matched NPU6 smoke workload. | Supported narrowly by the paired NPU6 runtime-hook smoke: hook-enabled minus hook-disabled is +0.08 ms TTFT p50, +10.03 ms TTFT p95, +0.04 ms latency p50, and +16.08 ms latency p95, with 12/12 success in both modes. | Same paired source directories and `.benchmarks/results/npu6_runtime_hook_pair_plan/summary.json`; next required evidence is memory/HBM snapshots and a larger workload/fault matrix. | Do not claim TPOT, memory, or full end-to-end profiler overhead yet; this is a small warmup-controlled smoke over one shared workload shape. |
| The profiler reduces time-to-root-cause compared with raw logs. | Not yet measured. | Human or scripted diagnosis comparison on real traces. | Do not claim performance improvement from profiler-only runs. |
