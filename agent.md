# Agent Handoff

The authoritative repository instructions are in `AGENTS.md`. This file is a
compatibility pointer only.

Keep the KV-recovery profiler default-off, preserve serving fail-open behavior,
validate evidence fail-closed, and use ordinary review, tests, CI, and Git
history. The former project-local approval, co-signature, attestation, and
digest-chain framework is retired and must not be recreated.

G1-G6 are development mechanism/capture results, not benchmark #95 accepted
performance evidence. The project remains `NOT_M0_PROVEN` until genuine blind
evaluation and the flat-summary comparison are complete.
